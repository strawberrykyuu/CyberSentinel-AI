# ============================================================
# seminar_project — requirements.txt
# Python >= 3.10 recommended
# Install with:  pip install -r requirements.txt
# ============================================================

# Core data & ML
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Image processing (malware byte→image pipeline from notebook)
Pillow>=10.0.0
imageio>=2.31.0
opencv-python-headless>=4.8.0   # headless = no GUI deps needed

# Streamlit UI
streamlit>=1.35.0

# Visualisation (used in Streamlit)
plotly>=5.18.0

# Utilities
python-dateutil>=2.8.2
